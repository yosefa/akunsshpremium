#!/bin/bash
if [[ -n $2 ]]; then
tgl=`date '+%C%y/%m/%d' -d "$tambah+$2 days"`
else
tgl=`date '+%C%y/%m/%d' -d "$tambah+1 days"`
fi
useradd -c "trial" -e $tgl -d /home/$1 -m -g akun -s /bin/false $1
pass="trial"
echo "$1:$pass" | chpasswd
expuser
cp -R /root/alluser.txt /home/coeg/